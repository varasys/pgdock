## The port to be exposed on the host
## comment the next line to not expose a port
HOSTPORT="0.0.0.0:5432"

## The docker image to use
PGIMAGE="postgres:9.6.4-alpine"

## Optionally override the docker container name
#NAME="my_custom_db_name"

## turn on data checksums when initdb creates a new cluster
## hos no effect for existing clusters
POSTGRES_INITDB_ARGS="--data-checksums"

## use the config file in the data_dir/config directory
## the path below is the path in the container
PGARGS="--config_file=/etc/postgresql/postgresql.conf"
